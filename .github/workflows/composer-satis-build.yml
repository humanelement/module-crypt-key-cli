name: Build Composer Satis
on:
  push
permissions:
  id-token: write
  contents: read
jobs:
  build:
    uses: humanelement/composer-repo/.github/workflows/composer-satis-package-build.yml@refs/heads/main
